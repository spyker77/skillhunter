build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn config.asgi:application -w 4 -k uvicorn.workers.UvicornWorker
