version: "3.9"

services:

  redis:
    image: redis:7-alpine

  rabbitmq:
    image: rabbitmq:3.11-management

  db:
    image: postgres:15.2-alpine
    command: -c max_wal_size=2GB
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
    volumes:
      - postgresql_data:/var/lib/postgresql/data/

  pgbouncer:
    image: edoburu/pgbouncer:latest
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/postgres
      - POOL_MODE=transaction
    ports:
      - "5432:5432"
    depends_on:
      - db

  web:
    env_file:
      - .env
    build: .
    environment:
      - PYTHONDEVMODE=1
    command: sh -c "python manage.py collectstatic --no-input --clear && gunicorn -b 0.0.0.0:8000 -w 8 -k uvicorn.workers.UvicornWorker core.asgi:application"
    volumes:
      - .:/code
      - static_volume:/code/staticfiles
      - media_volume:/code/mediafiles
    expose:
      - 8000
    depends_on:
      - redis
      - rabbitmq
      - db

  celery:
    env_file:
      - .env
    build: .
    command: celery -A core worker --without-heartbeat --without-gossip --without-mingle -l INFO
    volumes:
      - .:/code
      - static_volume:/code/staticfiles
      - media_volume:/code/mediafiles
    depends_on:
      - web

  nginx:
    build: ./nginx
    restart: on-failure
    volumes:
      - static_volume:/code/staticfiles
      - media_volume:/code/mediafiles
    ports:
      - "80:80"
    depends_on:
      - web

volumes:
  postgresql_data: null
  static_volume: null
  media_volume: null
